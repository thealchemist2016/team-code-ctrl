{
  "name": "xs-records-e2e",
  "version": "1.0.0",
  "description": "E2E tests for XS-Records Music Distribution Platform",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
